<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 앱 본체가 쓰는 권한이므로 main 에 명시한다.
         Flutter 템플릿은 이 권한을 debug·profile 소스셋에만 넣어주는데,
         그건 개발 중 Flutter 툴이 앱과 통신하기 위한 것이지 앱 자신을 위한
         게 아니다.

         릴리스 빌드에서도 지금까지는 권한이 있었다 — 다만 우리가 선언해서가
         아니라 mobile_scanner → ML Kit → transport-backend-cct 가 자기
         매니페스트에 INTERNET 을 달고 있어서 병합된 것이다. QR 스캐너를
         빼거나 그 의존성이 바뀌면 소리 없이 사라진다. 우리 API 호출이 남의
         라이브러리 매니페스트에 기대고 있을 이유가 없다. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Android 13+ 는 알림 표시에 런타임 권한이 필요하다.
         온보딩 완료 시점에 PushService 가 요청한다. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="iam"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 카카오 로그인 리다이렉트 수신.
             스킴의 키는 gradle이 환경변수 KAKAO_NATIVE_KEY 또는
             android/kakao.properties 에서 채운다 (build.gradle.kts 참고).
             키가 없으면 "kakao"만 남아 동작하지 않는데, 그 경우 Dart 쪽도
             개발용 로그인으로 폴백하므로 이 액티비티를 탈 일이 없다. -->
        <activity
            android:name="com.kakao.sdk.flutter.AuthCodeCustomTabsActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="kakao${kakaoNativeKey}" android:host="oauth" />
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- 백그라운드·종료 상태에서 OS 가 알림을 그릴 때 쓸 채널.
             같은 id 의 채널을 PushService 가 앱 시작 시 만든다. 이 값이
             앱이 만드는 채널과 다르면 백그라운드 알림이 기본 채널로 떨어져
             포그라운드 배너와 중요도·소리 설정이 갈린다. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="iam_default" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ 패키지 가시성. 없으면 `isKakaoTalkInstalled()`가 항상
             false를 돌려줘 카카오톡이 깔려 있어도 계정(웹뷰) 로그인으로 빠진다. -->
        <package android:name="com.kakao.talk" />
    </queries>
</manifest>

/// enum barrel — `import 'package:iam/data/enums/enums.dart';`
///
/// 전부 `.label` getter로 한국어 표시값을 갖는다.
/// UI는 raw enum 값을 절대 그리지 않는다(웹 `lib/enums/labels.ts`와 같은 원칙).
library;

export 'channel_enums.dart';
export 'profile_enums.dart';
export 'social_enums.dart';
